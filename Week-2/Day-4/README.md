# Week 2 – Day 4: Prefix Sum & Precomputation

## 🎯 Goal
Learn how prefix sums optimize repeated calculations and understand the idea of precomputation to improve time complexity.

---

## 📘 Topics Covered
- What is prefix sum
- Building a prefix sum array
- Range sum queries
- Avoiding repeated work using precomputation
- Using prefix logic to rethink existing problems

---

## 📂 Files in This Folder

| File Name | Description |
|---------|-------------|
| `prefix_sum_basics.cpp` | Manual construction of prefix sum array |
| `range_sum_bruteforce.cpp` | Brute force range sum queries |
| `range_sum_prefix.cpp` | Optimized range sum using prefix sum |
| `lc_303_range_sum_query_immutable.cpp` | LeetCode 303 – Range Sum Query (Immutable) |
| `max_subarray_prefix_thinking.cpp` | Revisiting maximum subarray using prefix idea |

---

## 🧠 Key Learnings
- Prefix sums store cumulative information
- Range sum queries can be answered in O(1) after preprocessing
- Precomputation trades space for time
- Many O(n²) problems reduce to O(n) using prefix arrays
- Thinking ahead avoids repeated calculations

---

## 🚀 Status
✅ Day-4 completed successfully  
📌 Focused on optimization through precomputation, not new data structures

---

## 🔜 Next
Day-5: Two pointers technique and index manipulation
